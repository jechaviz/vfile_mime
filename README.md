# vfile_mime

Pure V MIME type inference helpers for upload and import pipelines.

The library keeps explicit MIME types intact, and only infers a more specific
type when the declared value is empty or generic.
